[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ddda-steering"
version = "0.1.0"
description = "DDDA project intake, lifecycle tailoring, status and evidence-driven gate runtime"
requires-python = ">=3.11"
dependencies = ["ruamel.yaml>=0.18,<0.19"]

[project.scripts]
ddda-steering = "ddda_steering.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
