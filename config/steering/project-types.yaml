schema_version: 1
project_types:
  portfolio-program:
    label: Portfolio nebo transformační program
    stages: [align, discover, decompose, strategize, connect, organize, define, code]
    extensions: [program-lifecycle, domain-portfolio, wardley-optional]
    rationale:
      align: Potřebuje společný programový záměr, rozhodovací vlastníky a měřitelné outcomes.
      discover: Vyžaduje enterprise discovery napříč více value streams.
      decompose: Rozlišuje domény, subdomény a kandidátní bounded contexts.
  greenfield-product:
    label: Nový digitální produkt
    stages: [align, discover, decompose, strategize, connect, organize, define, code]
    extensions: [minimum-valuable-slice]
  legacy-modernization:
    label: Inkrementální modernizace legacy systému
    stages: [align, discover, decompose, strategize, connect, organize, define, code]
    extensions: [as-is-evidence, seams-and-acl, migration-slices, decommission]
  legacy-transformation:
    label: Business a IT transformace
    stages: [align, discover, decompose, strategize, connect, organize, define, code]
    extensions: [transition-states, operating-model, migration-waves]
  integration-landscape:
    label: Integrace a data ownership
    stages: [align, discover, decompose, strategize, connect, organize, define]
    extensions: [e2e-scenarios, source-of-truth, failure-modes]
  purchased-product-adoption:
    label: SaaS nebo COTS adopce
    stages: [align, discover, decompose, strategize, connect, organize, define]
    extensions: [fit-gap, vendor-boundary, acl, exit-plan]
  domain-discovery:
    label: Časově omezené poznání domény
    stages: [align, discover, decompose]
    extensions: [glossary, hotspots, validation-backlog]
  architecture-review:
    label: Architektonické review
    stages: [align, discover, decompose, strategize, connect, organize, define]
    extensions: [findings, quality-attributes, adr-backlog]
  operating-model-and-teams:
    label: Ownership a Team Topologies
    stages: [align, discover, decompose, strategize, connect, organize]
    extensions: [cognitive-load, interaction-modes, decision-authority]
  bounded-context-design:
    label: Detailní návrh bounded contextu
    stages: [align, discover, decompose, strategize, connect, organize, define, code]
    extensions: [design-level-eventstorming, aggregates, invariants, ports]
